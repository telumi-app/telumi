/** Heartbeat TTL in seconds — TV is ONLINE if last seen within this window */
export const HEARTBEAT_TTL_SECONDS = 90;

/** Default slot duration in seconds */
export const DEFAULT_SLOT_DURATION_SECONDS = 15;

/** Default ads fill rate percentage */
export const DEFAULT_FILL_RATE_PERCENT = 20;

/** Default credit expiration in days */
export const DEFAULT_CREDIT_EXPIRATION_DAYS = 90;
